import { InjectionToken } from '@angular/core';

/** Injected in content projected under `sa-tool-scaffold` for tool-scoped helpers. */
export const TOOL_SCAFFOLD_TOOL_ID = new InjectionToken<string>('TOOL_SCAFFOLD_TOOL_ID');
